import { TextDocument } from "vscode-languageserver-types";
export interface IFileContentProvider {
    getContent(filePath: string, relativeTo: string): string;
}
export declare class FileContentProvider implements IFileContentProvider {
    private inmemoryStorage;
    constructor(inmemoryStorage: Record<string, TextDocument>);
    getContent(filePath: string, relativeTo: string): string;
}
