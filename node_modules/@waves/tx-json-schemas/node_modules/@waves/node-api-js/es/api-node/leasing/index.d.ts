import { ILeaseTransaction, IWithApiMixin } from '@waves/ts-types';
import { TLong } from '../../interface';
/**
 * GET /leasing/active/{address}
 * Get all active leases for an address
 */
export declare function fetchActive(base: string, address: string, options?: RequestInit): Promise<Array<ILeaseTransaction<TLong> & IWithApiMixin & IActive>>;
export interface IActive {
    feeAssetId: string | null;
}
