import { IMessage, IGetMessageOptions } from './interfaces';
export declare class MessageKeeper {
    private readonly _messages;
    private readonly _maxLength;
    constructor(maxLength: number);
    push(message: IMessage): void;
    getMessages(options?: IGetMessageOptions): Array<IMessage>;
}
