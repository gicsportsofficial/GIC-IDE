import { TLong } from '../../interface';
export declare function fetchNodeStatus(base: string, options?: RequestInit): Promise<INodeStatus<TLong>>;
export declare function fetchNodeVersion(base: string, options?: RequestInit): Promise<INodeVersion>;
export interface INodeStatus<LONG> {
    blockchainHeight: LONG;
    stateHeight: LONG;
    updatedTimestamp: LONG;
    updatedDate: string;
}
export interface INodeVersion {
    version: string;
}
