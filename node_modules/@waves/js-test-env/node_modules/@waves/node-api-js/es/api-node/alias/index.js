import request from '../../tools/request';
export function fetchByAlias(base, alias, options = Object.create(null)) {
    return request({
        base,
        url: `/alias/by-alias/${alias}`,
        options
    });
}
export function fetchByAddress(base, address, options = Object.create(null)) {
    return request({
        base,
        url: `/alias/by-address/${address}`,
        options
    });
}
//# sourceMappingURL=index.js.map